import { Box } from "@mui/material";
import { styled } from "@mui/system";

const CoreDescriptionWrapper = styled(Box)(({ theme }) => ({
  margin: theme.spacing(1.25),
}));

export default CoreDescriptionWrapper;
