import { Box } from "@mui/material";
import { styled } from "@mui/system";

const ChoiceButtonsContainer = styled(Box)({
  display: "flex",
  width: "100%",
  justifyContent: "center",
});

export default ChoiceButtonsContainer;
